import { RedirectionElements } from './redirection-elements';

/**
 * Unique ids for footer elements that can redirect the user.
 */
export const HEADER_IDS: RedirectionElements = {
    phone: "#header-contact-phone",
    phoneProduction: "#header-contact-phone-production",
    location: "#header-contact-localization",
    mail: "#header-contact-mail"
}