/**
 * Model for the object that contains elements ids for redirection.
 */
export interface RedirectionElements {
    location: string,
    phone: string,
    phoneProduction: string,
    mail: string
}